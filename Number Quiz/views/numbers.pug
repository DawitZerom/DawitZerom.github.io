div 
    h1 The Number Quiz 
    h4 Your current score is #{score}
    h3 Guess the next number in the seqauence 
    - const array = {{3,1,1...}, {1,2,4,5,3...}, {5,4,5,3...}};

    - for ( var i = 1; i<= 5; i++ ){
        p = array[i]

        label Your answer:  
        input (type = "text")
        br
        input (type = "submit")
   - }
   app.get('/api', function(req, res){
        res.locals = { title: 'CS572' };
        res.render('index');
    });
    
    